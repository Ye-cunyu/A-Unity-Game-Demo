using UnityEngine;

public class SkeletonGroundState : SkeletonState
{
    public SkeletonGroundState(Skeleton _skeleton, SkeletonStateMachine _stateMachine, string _animBoolName)
    : base(_skeleton, _stateMachine, _animBoolName)
    {

    }
}
